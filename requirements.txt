fastapi>=0.110
uvicorn>=0.29
pypdf>=4.2
pillow>=10.3
numpy>=1.26
python-docx>=1.1
lxml>=5.0
httpx>=0.27
reportlab>=4.5  # 검증 환경은 4.5.0 (requirements.lock.txt) — 5.x도 허용하되 실검증 기준은 lock 참조
olefile>=0.47
rhwp-python>=0.7  # HWP 읽기 엔진 + 내보내기 미리보기 렌더러 (선택이지만 권장)
PyMuPDF>=1.24  # 고정밀 PDF 인식(문항 세그멘테이션·이미지/그림 위치·수식 영역). 없으면 pypdf 텍스트 경로로 자동 폴백
# OCR을 쓰려면 tesseract 설치 후: pip install pytesseract

